import numpy as np
import matplotlib.pyplot as plt

#dim = int(input("dimensión: "))

#cont = 1
#num = 1
#lista = [1]

#while (cont < dim):
#     if(cont%2 == 0):
#          num = 1
#     else:
#          num = 0
#     lista.append(num)
#     cont = cont + 1

#print(lista)

#a = np.array([1, 0, 1])
#b = np.array([0, 1, 0])

#a = np.array([lista])
#b = np.array([lista])

#tablero = np.array([a, b], dtype=int)
#plt.imshow(tablero, cmap='gray', vmin=0, vmax=1)
#print(tablero)
#plt.show()

chess = np.zeros((8,8))
#un solo randomize
chess[1::2, 0::2] = 1
chess[0::2, 1::2] = 1
plt.imshow(chess, cmap='gray', vmin=0, vmax=1)
plt.show()
